[project]
name = "pii-arena"
version = "1.0.3"
description = "PII Arena: reproducible PII and secret detector measurements across Russian, English and multilingual datasets"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false
