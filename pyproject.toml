[project]
name = "pii-secrets-benchmark"
version = "1.0.3"
description = "Reproducible PII and secret detection measurements across Russian, English and multilingual datasets"
requires-python = ">=3.12,<3.13"
dependencies = []

[dependency-groups]
plots = ["matplotlib==3.10.8"]

[tool.uv]
package = false
